version: "3.9"
services:
  automl:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8501:8501"
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
